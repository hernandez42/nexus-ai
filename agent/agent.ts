import { defineAgent } from "eve";

export default defineAgent({
  description: "Nexus Agent — recursive self-improving cognitive system with memory, evolution, and self-inspection",
  model: "openai/gpt-4o",
});
